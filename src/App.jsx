// src/App.jsx
import Home from './pages/Home'; // Import the Home page component
import './App.css'; // We can keep this for any global App-specific styles if needed, or remove it later.

// The main App component now simply renders the Home page.
function App() {
  return (
    <Home />
  );
}

export default App;
